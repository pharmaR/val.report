# Create a

Vectorize assessment to be able to add it to a report for a package or
the PACKAGES file.

## Usage

``` r
assessment(assessment)
```

## Arguments

- assessment:

  An object created by `pkg_assess()` (`list_of_pkg_metric`)

## Value

A data.frame with simplified objects but no scoring of the assessment.
